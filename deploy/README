docker build -f Dockerfile_base --progress=plain -t metaboreport_base .
docker build -f Dockerfile --progress=plain -t metaboreport:latest .
docker run -p 80:80 metaboreport:latest
# then go to 127.0.0.1:80